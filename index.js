//Get the startApp class
const startApp = require("./lib/startApp");

//Creates a new variable from StartApp
const start = new startApp();

//Runs the begin method on the new variable 'start'
start.begin();